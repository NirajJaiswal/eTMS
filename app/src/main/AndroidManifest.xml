<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/bus_service_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/bus_service_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.ETMS"
        tools:targetApi="31">
        <activity
            android:name=".WelcomeScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LandingScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.ETMS.NoActionBar" />
    </application>

</manifest>